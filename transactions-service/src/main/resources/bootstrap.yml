server:
  port: 9000

spring:
  application:
    name: transactions

  cloud:
    consul:
      host: localhost
      port: 8500

      config:
        failFast: true # Enable connection retry
        profileSeparator: '::'

      discovery:
        failFast: false